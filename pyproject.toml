[project]
name = "enterprise-agent-copilot"
version = "0.1.0"
description = "Enterprise Agent Copilot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.133.0",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.41.0",
]
